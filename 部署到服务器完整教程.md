# 🚀 部署到服务器完整教程

## 📋 部署前准备

### 需要的信息
- ✅ 服务器 IP 地址
- ✅ SSH 用户名和密码（或密钥）
- ✅ 服务器上的网站目录路径（例如：`/var/www/html` 或 `/www/wwwroot`）
- ✅ Git 仓库地址（如果使用 Git 部署）

### 需要的软件
- ✅ SSH 客户端（Windows 自带 or PuTTY）
- ✅ FTP/SFTP 客户端（如 FileZilla、WinSCP）

---

## 🎯 部署方案选择

### 方案1：构建后上传（最简单，推荐新手）

**流程：** 本地构建 → 压缩文件 → 上传到服务器 → 解压

**优点：**
- ✅ 简单直接
- ✅ 不需要在服务器安装 Node.js
- ✅ 上传的是静态文件，体积小

**适合：** 初次部署、虚拟主机用户

---

### 方案2：Git + 服务器构建（推荐长期使用）

**流程：** 推送到 Git → 服务器拉取 → 服务器构建 → 自动部署

**优点：**
- ✅ 更新方便
- ✅ 有版本控制
- ✅ 可以自动化

**适合：** 有服务器管理权限、懂基本 Linux 命令

---

## 📦 方案1：构建后上传部署（详细步骤）

### 步骤1：本地构建项目

```batch
# 1. 打开命令提示符（CMD）
cd "D:\cursor\project8  blog\Mizuki-master"

# 2. 清理之前的构建
rmdir /s /q dist

# 3. 构建项目
npm run build

# 4. 等待构建完成，会生成 dist 文件夹
```

**验证构建成功：**
- 检查是否存在 `dist` 文件夹
- `dist` 文件夹里应该有 `index.html` 等文件

### 步骤2：压缩 dist 文件夹

**使用 Windows 自带压缩工具：**
1. 右键点击 `dist` 文件夹
2. 选择"发送到" → "压缩文件夹"
3. 会生成 `dist.zip` 文件

**或者使用 7-Zip/WinRAR：**
1. 右键 `dist` 文件夹
2. 选择"添加到压缩文件"
3. 压缩格式选择 `.zip` 或 `.tar.gz`

### 步骤3：上传到服务器

#### 方法A：使用 FileZilla（推荐）

1. **下载并安装 FileZilla**
   - 下载地址：https://filezilla-project.org/

2. **连接到服务器**
   ```
   主机: sftp://你的服务器IP
   用户名: 你的SSH用户名
   密码: 你的SSH密码
   端口: 22
   ```

3. **上传文件**
   - 左侧：找到本地的 `dist.zip`
   - 右侧：导航到服务器网站目录（如 `/var/www/html/blog`）
   - 拖动 `dist.zip` 到右侧上传

#### 方法B：使用 WinSCP

1. **下载并安装 WinSCP**
   - 下载地址：https://winscp.net/

2. **新建连接**
   ```
   文件协议: SFTP
   主机名: 你的服务器IP
   端口: 22
   用户名: 你的SSH用户名
   密码: 你的SSH密码
   ```

3. **上传文件**
   - 将 `dist.zip` 拖到服务器目录

#### 方法C：使用 SCP 命令（命令行）

```batch
# Windows PowerShell 或 CMD
scp dist.zip 用户名@服务器IP:/var/www/html/blog/

# 例如：
scp dist.zip root@192.168.1.100:/var/www/html/blog/
```

### 步骤4：SSH 连接到服务器

**使用 Windows 自带 SSH：**
```batch
# 打开 PowerShell 或 CMD
ssh 用户名@服务器IP

# 例如：
ssh root@192.168.1.100

# 输入密码后回车
```

**或使用 PuTTY：**
1. 打开 PuTTY
2. 输入服务器 IP
3. 端口选 22
4. 点击 Open
5. 输入用户名和密码

### 步骤5：解压并部署

```bash
# 1. 进入网站目录
cd /var/www/html/blog

# 2. 备份旧版本（如果有）
mv index.html index.html.bak 2>/dev/null

# 3. 解压文件
unzip -o dist.zip

# 4. 将 dist 里的文件移到当前目录
mv dist/* .
rmdir dist

# 5. 删除压缩包
rm dist.zip

# 6. 设置文件权限
chmod -R 755 .
chown -R www-data:www-data .  # 如果是 Nginx
# 或
chown -R apache:apache .      # 如果是 Apache
```

### 步骤6：配置 Web 服务器

#### 如果使用 Nginx

创建或编辑配置文件：
```bash
nano /etc/nginx/sites-available/blog
```

配置内容：
```nginx
server {
    listen 80;
    server_name yourdomain.com;  # 改成你的域名或IP
    
    root /var/www/html/blog;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 静态资源缓存
    location ~* \.(css|js|jpg|jpeg|png|gif|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

启用配置并重启：
```bash
ln -s /etc/nginx/sites-available/blog /etc/nginx/sites-enabled/
nginx -t          # 测试配置
systemctl reload nginx
```

#### 如果使用 Apache

创建或编辑 `.htaccess`：
```bash
nano /var/www/html/blog/.htaccess
```

内容：
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

重启 Apache：
```bash
systemctl reload apache2
```

### 步骤7：访问网站

在浏览器打开：
```
http://你的服务器IP/blog/
或
http://你的域名/
```

---

## 🔄 方案2：Git + 自动部署（推荐）

### 步骤1：准备 Git 仓库

#### 1.1 初始化本地仓库（如果还没有）

```batch
cd "D:\cursor\project8  blog\Mizuki-master"

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"
```

#### 1.2 创建远程仓库

**GitHub：**
1. 访问 https://github.com/
2. 点击 "New repository"
3. 填写仓库名称（例如：my-blog）
4. 选择 Public 或 Private
5. 点击 "Create repository"

**Gitee（国内访问快）：**
1. 访问 https://gitee.com/
2. 点击 "+"  → "新建仓库"
3. 填写信息并创建

#### 1.3 关联远程仓库并推送

```batch
# 关联远程仓库
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 或使用 Gitee
git remote add origin https://gitee.com/你的用户名/你的仓库名.git

# 推送代码
git branch -M main
git push -u origin main
```

### 步骤2：服务器端配置

#### 2.1 安装必要软件

```bash
# SSH 连接到服务器
ssh 用户名@服务器IP

# 安装 Node.js（如果没有）
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version

# 安装 Git（如果没有）
sudo apt-get install git
```

#### 2.2 克隆项目到服务器

```bash
# 创建项目目录
sudo mkdir -p /www/blog-source
cd /www/blog-source

# 克隆仓库
sudo git clone https://github.com/你的用户名/你的仓库名.git .

# 如果是私有仓库，需要输入用户名和密码
# 或者配置 SSH 密钥
```

#### 2.3 安装依赖并首次构建

```bash
# 安装依赖
sudo npm install

# 构建项目
sudo npm run build

# 复制到网站目录
sudo mkdir -p /var/www/html/blog
sudo cp -r dist/* /var/www/html/blog/
```

### 步骤3：创建自动部署脚本

```bash
# 创建部署脚本
sudo nano /www/blog-source/deploy.sh
```

粘贴以下内容：
```bash
#!/bin/bash
echo "========================================="
echo "开始部署博客..."
echo "========================================="

# 设置变量
PROJECT_DIR="/www/blog-source"
WEB_DIR="/var/www/html/blog"
BACKUP_DIR="/www/backup"

# 进入项目目录
cd $PROJECT_DIR

# 拉取最新代码
echo "📥 拉取最新代码..."
git pull origin main

if [ $? -ne 0 ]; then
    echo "❌ 拉取代码失败！"
    exit 1
fi

# 安装依赖
echo "📦 安装依赖..."
npm install

# 清理旧构建
echo "🧹 清理旧构建..."
rm -rf dist

# 构建项目
echo "🔨 构建项目..."
npm run build

if [ $? -ne 0 ]; then
    echo "❌ 构建失败！"
    exit 1
fi

# 备份当前版本
echo "📦 备份当前版本..."
if [ -d "$WEB_DIR" ]; then
    BACKUP_NAME="backup-$(date +%Y%m%d-%H%M%S)"
    mkdir -p $BACKUP_DIR
    cp -r $WEB_DIR $BACKUP_DIR/$BACKUP_NAME
    echo "✅ 已备份到: $BACKUP_DIR/$BACKUP_NAME"
fi

# 部署新版本
echo "🚀 部署新版本..."
rm -rf $WEB_DIR/*
cp -r dist/* $WEB_DIR/

# 设置权限
chmod -R 755 $WEB_DIR
chown -R www-data:www-data $WEB_DIR

echo "========================================="
echo "✅ 部署完成！"
echo "========================================="
echo "部署时间: $(date)"
echo "Git 版本: $(git log -1 --format='%h - %s')"
```

保存并赋予执行权限：
```bash
sudo chmod +x /www/blog-source/deploy.sh
```

### 步骤4：测试部署脚本

```bash
# 运行部署脚本
sudo /www/blog-source/deploy.sh
```

如果成功，访问你的网站验证！

### 步骤5：后续更新流程

**以后每次更新文章，只需要：**

```batch
# 在本地 Windows 上
cd "D:\cursor\project8  blog\Mizuki-master"

# 添加更改
git add .

# 提交
git commit -m "新增文章：XXX"

# 推送
git push
```

**然后在服务器上：**

```bash
# SSH 连接到服务器
ssh 用户名@服务器IP

# 运行部署脚本
sudo /www/blog-source/deploy.sh
```

### 步骤6：（可选）设置自动部署

#### 方法A：使用 GitHub Actions

在项目中创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /www/blog-source
            sudo ./deploy.sh
```

在 GitHub 仓库设置中添加 Secrets：
- `SERVER_HOST`: 服务器 IP
- `SERVER_USER`: SSH 用户名
- `SERVER_PASSWORD`: SSH 密码

#### 方法B：使用 Webhook

安装 webhook 工具：
```bash
# 安装 webhook
sudo apt-get install webhook

# 创建配置
sudo nano /etc/webhook.conf
```

配置内容：
```json
[
  {
    "id": "blog-deploy",
    "execute-command": "/www/blog-source/deploy.sh",
    "command-working-directory": "/www/blog-source",
    "response-message": "Deploying blog...",
    "trigger-rule": {
      "match": {
        "type": "payload-hash-sha1",
        "secret": "your-secret-key",
        "parameter": {
          "source": "header",
          "name": "X-Hub-Signature"
        }
      }
    }
  }
]
```

启动 webhook：
```bash
sudo webhook -hooks /etc/webhook.conf -verbose
```

在 GitHub/Gitee 仓库设置中添加 Webhook：
```
URL: http://你的服务器IP:9000/hooks/blog-deploy
Secret: your-secret-key
```

---

## 🔍 部署后验证

### 检查清单

- [ ] 网站能正常访问
- [ ] 首页显示正常
- [ ] 图片能正常加载
- [ ] 点击文章能跳转
- [ ] 导航菜单工作正常
- [ ] 移动端显示正常

### 常见问题

#### Q1: 访问网站显示 403 Forbidden
```bash
# 检查权限
ls -la /var/www/html/blog

# 修正权限
sudo chmod -R 755 /var/www/html/blog
sudo chown -R www-data:www-data /var/www/html/blog
```

#### Q2: 页面刷新后显示 404
需要配置服务器重写规则（见上面 Nginx/Apache 配置）

#### Q3: 图片显示不出来
```bash
# 检查图片文件是否存在
ls /var/www/html/blog/_astro/

# 检查 base 路径配置
cat astro.config.mjs
```

---

## 📝 快速参考

### 本地命令（Windows）

```batch
# 构建项目
npm run build

# 提交代码
git add .
git commit -m "更新"
git push
```

### 服务器命令（Linux）

```bash
# 连接服务器
ssh 用户名@IP地址

# 更新部署
sudo /www/blog-source/deploy.sh

# 查看日志
tail -f /var/log/nginx/access.log

# 重启服务
sudo systemctl reload nginx
```

---

## 🎉 完成！

现在你的博客已经成功部署到服务器了！

**后续更新只需要：**
1. 本地修改文章
2. Git 提交推送
3. 服务器运行 `deploy.sh`

---

需要帮助？检查以下内容：
- 服务器是否安装了 Node.js
- 网站目录权限是否正确
- Web 服务器配置是否正确
- 防火墙是否开放 80 端口

